{
  "name": "fixed-to-do-list",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "main": "main.cjs",
  "scripts": {
    "dev": "vite",
    "preview": "vite preview",
    "start": "npm run pre-build && electron .",
    "build": "npm run pre-build && node package.cjs",
    "pre-build": "tsc && vite build"
  },
  "devDependencies": {
    "electron": "^26.3.0",
    "electron-packager": "^17.1.2",
    "sass": "^1.69.0",
    "typescript": "^5.2.2",
    "vite": "^4.4.11"
  }
}
